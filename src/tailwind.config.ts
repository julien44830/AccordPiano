import type { Config } from "tailwindcss";


const config: Config = {
  theme: {
    extend: {
      colors: {
        brand: {
          primary: "#C77966",
          dark: "#0d0d0d",
          light: "#f5f5f5",
        },
      },
    },
  },

  plugins: [],
};
export default config;